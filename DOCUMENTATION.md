# 📖 Документация на проекта

**Автор:** Божидар Георгиев  
**Email:** 136ikz@unibit.bg  
**Дата:** 11 ноември 2025

---

## 📋 Съдържание

1. [Преглед на проекта](#преглед-на-проекта)
2. [Архитектура](#архитектура)
3. [Технологии](#технологии)
4. [Модели на данни](#модели-на-данни)
5. [Бизнес логика](#бизнес-логика)
6. [Контролери и изгледи](#контролери-и-изгледи)
7. [Изпълнение на изискванията](#изпълнение-на-изискванията)

---

## 🎯 Преглед на проекта

Система за управление на библиотека, разработена с ASP.NET Core MVC (.NET 8). Проектът предоставя пълна функционалност за управление на библиотечни ресурси, членове, заемания и събития.

### Основни функции:
- ✅ Управление на книги, вестници, списания и периодични издания
- ✅ Регистрация и профили на членове
- ✅ Система за заемане и връщане на книги
- ✅ Автоматично изчисляване на глоби
- ✅ Организиране на библиотечни събития
- ✅ Ролеви достъп (Admin, Librarian, Member)
- ✅ Отчети и статистики

---

## 🏗️ Архитектура

Проектът следва **многослойна архитектура** (Layered Architecture):

```
┌─────────────────────────────────────────┐
│   Presentation Layer (MVC)              │
│   - Controllers                          │
│   - Views (Razor)                        │
│   - ViewModels                           │
└─────────────────┬───────────────────────┘
                  │
┌─────────────────▼───────────────────────┐
│   Business Logic Layer                  │
│   - Services                             │
│   - Validation                           │
└─────────────────┬───────────────────────┘
                  │
┌─────────────────▼───────────────────────┐
│   Data Access Layer                     │
│   - Repositories                         │
│   - Unit of Work                         │
│   - Entity Framework Core                │
└─────────────────┬───────────────────────┘
                  │
┌─────────────────▼───────────────────────┐
│   Database Layer                        │
│   - SQL Server                           │
│   - Models/Entities                      │
└─────────────────────────────────────────┘
```

### Структура на проекта:

**LibraryManagementSystem/** - Главно уеб приложение
- `Program.cs` - Входна точка и конфигурация
- `Controllers/` - MVC контролери
- `Views/` - Razor изгледи
- `Areas/` - Модулни секции (Admin, Member)
- `wwwroot/` - Статични файлове

**Library.Models/** - Модели на данните (Entities)
- Domain models: `Book`, `Author`, `Member`, `Borrowing` и др.

**Library.Repositories/** - Data Access Layer
- `ApplicationDbContext.cs` - EF Core контекст
- `Implementation/` - Имплементации
- `Interfaces/` - Интерфейси
- `Migrations/` - Database migrations

**Library.Services/** - Business Logic Layer
- Сервизи за бизнес логика: `BookService`, `MemberService` и др.
- Интерфейси: `IBookService`, `IMemberService` и др.

**Library.ViewModels/** - View Models за презентация

**Library.Validation/** - Валидационна логика

**Library.Utilities/** - Помощни класове

**Library.Mappings/** - AutoMapper профили

---

## 💻 Технологии

### Backend:
- **ASP.NET Core MVC** - .NET 8
- **Entity Framework Core** - ORM за достъп до данни
- **C#** - Основен програмен език
- **SQL Server** - Релационна база данни

### Frontend:
- **Razor Views** - Templating engine
- **Bootstrap 5** - CSS framework за стилизация
- **JavaScript** - Клиентска логика

### Допълнителни:
- **ASP.NET Core Identity** - Автентикация и оторизация
- **AutoMapper** - Object mapping
- **CloudScribe** - Pagination компонент
- **LINQ** - Заявки към данни

---

## 📊 Модели на данни

### Основни entities:

#### 1. **Book** (Книга)
Наследява `LibraryItem`, добавя ISBN и Edition

```csharp
public class Book : LibraryItem
{
    public string ISBN { get; set; }
    public string Edition { get; set; }
}
```

#### 2. **LibraryItem** (Базов клас за артикули)
Абстрактен клас с общи полета за всички артикули

```csharp
public abstract class LibraryItem
{
    public int Id { get; set; }
    public string ItemCode { get; set; }
    public string Title { get; set; }
    public int? PublishedYear { get; set; }
    // ... и други полета
}
```

#### 3. **Author** (Автор)
Информация за автори на публикации

```csharp
public class Author
{
    public int Id { get; set; }
    public string Name { get; set; }
    public int CountryId { get; set; }
    public string Biography { get; set; }
}
```

#### 4. **Member** (Член)
Профил на читател в библиотеката

```csharp
public class Member
{
    public int Id { get; set; }
    public DateTime MembershipDate { get; set; }
    public int? MembershipTypeId { get; set; }
    public string ApplicatioUserId { get; set; }
}
```

### Връзки между таблици:

- **Един към много (1:N)**
  - `Language` → `LibraryItem`
  - `Category` → `LibraryItem`
  - `Member` → `Borrowing`

- **Много към много (M:N)**
  - `Author` ↔ `LibraryItem` (чрез `ItemAuthor`)
  - `Member` ↔ `LibraryEvent` (чрез `EventParticipant`)

---

## 🔧 Бизнес логика

### BookService - Управление на книги

**Основни методи:**

```csharp
// CREATE - Добавяне на книга с копия
InsertBookResult InsertBook(BookViewModel book)

// READ - Извличане на всички книги със страниране
PagedResult<BookViewModel> GetAll(int pageNumber, int pageSize)

// READ - Извличане по ID
BookViewModel GetBookById(int bookId)

// UPDATE - Редактиране на книга
void UpdateBook(BookViewModel book)

// DELETE - Изтриване на книга
void DeleteBook(int id)

// SEARCH - Търсене по име
PagedResult<BookViewModel> GetBookByName(string name, int pageNumber, int pageSize)

// UTILITY - Генериране на уникален код
string GenerateNextBookCode()
```

**Особености:**
- Автоматично генериране на кодове (ITD-BK-00001)
- Създаване на множество физически копия
- Проверка за дублиращи се записи
- Страниране на резултатите

---

## 🎨 Контролери и изгледи

### HomeController
Управлява началната страница и общи функции

**Actions:**
- `Index()` - Начална страница с последни събития
- `Privacy()` - Страница за поверителност
- `Error()` - Обработка на грешки

### AboutController
Страница "За нас" с различни секции

**Actions (Partial Views):**
- `WhoWeAre()` - Кои сме ние
- `VisionMission()` - Визия и мисия
- `AimsObjectives()` - Цели и задачи
- `Background()` - История
- `OurServices()` - Услуги

### Layout структура:

```
_Layout.cshtml                  ← Основен layout
├── _Header (Partial)           ← Навигация
├── @RenderBody()               ← Съдържание
│   ├── Index.cshtml
│   ├── About.cshtml
│   └── ...
├── _Footer (Partial)           ← Footer
└── @RenderSection("Scripts")  ← JavaScript секция
```

---

## ✅ Изпълнение на изискванията

### 1. Структура и функционалност ✔️

- ✅ **Начална страница** - `HomeController/Index`
- ✅ **Страница "За нас"** - `AboutController` с информация за проекта
- ✅ **MVC архитектура** - Controllers, Models, Views
- ✅ **Навигационно меню** - В `_Layout.cshtml`
- ✅ **Динамично съдържание** - Над 10 страници (Book, Member, Events, Borrowing и др.)
- ✅ **.NET 8** - Последна версия

### 2. Работа с база данни ✔️

- ✅ **Entity Framework Core** - ApplicationDbContext
- ✅ **CRUD функционалност** - За книги, членове, автори, събития и още
- ✅ **Валидации** - Data annotations в Models + client-side validation
- ✅ **Обработка на грешки** - Try-catch блокове и error handling

### 3. Потребителски интерфейс ✔️

- ✅ **Bootstrap 5** - Модерен и responsive дизайн
- ✅ **Razor Views** - Всички изгледи
- ✅ **Layout** - `_Layout.cshtml` основен шаблон
- ✅ **Partial Views** - `_AboutWhoWeAre`, `_Header`, `_Footer` и др.
- ✅ **Sections** - `@section Scripts` в layout-а

### 4. Допълнителни функции ✔️

- ✅ **Връзки 1:N и M:N** - Множество връзки между таблици
- ✅ **Търсене/филтриране** - `GetBookByName()` в BookService
- ✅ **Регистрация и логин** - ASP.NET Core Identity
- ✅ **Role-based access** - Admin, Librarian, Member роли

---

## 📝 Коментари в кода

Всички коментари са написани на български език и съдържат:
- **Header коментари** - В началото на всеки файл с автор и описание
- **XML документация** - За всички публични методи и класове
- **Inline коментари** - За сложна логика
- **Section маркери** - За организиране на кода

**Пример:**
```csharp
/*
 * ========================================================================================================
 * МОДЕЛ: КНИГА (BOOK)
 * ========================================================================================================
 * Автор: Божидар Георгиев
 * Email: 136ikz@unibit.bg
 * ...
 */

/// <summary>
/// Модел за книга в библиотечната система
/// Автор: Божидар Георгиев (136ikz@unibit.bg)
/// </summary>
public class Book : LibraryItem
{
    // ...
}
```

---

## 🚀 Стартиране на проекта

1. **Отваряне на проекта**
   ```bash
   cd "c:\Users\bobss\OneDrive\Desktop\Библиотека"
   ```

2. **Restore на пакети**
   ```bash
   dotnet restore
   ```

3. **Конфигуриране на connection string**
   Редактирайте `appsettings.json`:
   ```json
   "ConnectionStrings": {
     "DefaultConnection": "Server=...;Database=LibraryDB;..."
   }
   ```

4. **Прилагане на миграции**
   ```bash
   dotnet ef database update
   ```

5. **Стартиране на приложението**
   ```bash
   dotnet run --project LibraryManagementSystem
   ```

---

## 📚 Използвани Design Patterns

1. **MVC Pattern** - Разделение на логиката на Model, View, Controller
2. **Repository Pattern** - Абстракция над достъпа до данни
3. **Unit of Work Pattern** - Централизиране на транзакциите
4. **Dependency Injection** - Инжектиране на зависимости
5. **Factory Pattern** - За създаване на обекти
6. **Table Per Hierarchy (TPH)** - EF наследяване за LibraryItem

---

## 👨‍💻 За контакт

**Разработчик:** Божидар Георгиев  
**Email:** 136ikz@unibit.bg  
**Институция:** УНИБИТ  
**Учебна година:** 2024/2025

---

*Документацията е създадена като част от изискванията за курсовия проект.*
