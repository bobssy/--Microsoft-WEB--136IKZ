# 🚀 Бърза инсталация

**Автор:** Божидар Георгиев  
**Email:** 136ikz@unibit.bg

---

## 📦 Два начина за инсталация

### Вариант 1: Автоматична инсталация (Препоръчително) ⚡

**За Windows:**

1. **Отворете папката на проекта**
2. **Двоен клик на `Install.bat`**
3. **Следвайте инструкциите на екрана**
4. **Готово!** 🎉

Или чрез PowerShell (като администратор):
```powershell
.\Install-LibrarySystem.ps1
```

---

### Вариант 2: Ръчна инсталация 🔧

**Предварителни изисквания:**
- .NET 8 SDK

**Команди:**
```powershell
# 1. Инсталиране на зависимости
dotnet restore

# 2. Добавяне на SQLite
cd LibraryManagementSystem
dotnet add package Microsoft.EntityFrameworkCore.Sqlite

# 3. Създаване на базата данни
dotnet ef database update

# 4. Стартиране
dotnet run
```

**Отворете браузър:**
```
https://localhost:5001
```

---

## 🔑 Данни за вход

След инсталация използвайте:

**Администратор:**
```
Потребителско име: adminuser@gmail.com
Парола: Admin@123
```

**Библиотекар:**
```
Email: librarian@library.bg
Парола: Librarian123!
```

**Член (читател):**
```
Email: member@library.bg
Парола: Member123!
```

---

## 🗄️ База данни

По подразбиране се използва **SQLite** (файл `LibrarySystem.db`).

**Предимства на SQLite:**
- ✅ Не изисква инсталация на SQL Server
- ✅ Автоматично се създава при първо стартиране
- ✅ Портативна - цялата база е в един файл
- ✅ Идеална за развойна среда и демонстрации

**За промяна на SQL Server** (опционално):

Редактирайте `LibraryManagementSystem/appsettings.json`:
```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Server=localhost;Database=LibraryDB;Trusted_Connection=True;TrustServerCertificate=True"
  }
}
```

И в `Program.cs` променете:
```csharp
// Вместо UseSqlite използвайте UseSqlServer
options.UseSqlServer(...)
```

---

## ❓ Чести проблеми

### Проблем: "dotnet command not found"
**Решение:** 
1. Свалете .NET 8 SDK от: https://dotnet.microsoft.com/download
2. Инсталирайте го
3. Рестартирайте компютъра
4. Опитайте отново

### Проблем: "Port 5001 is already in use"
**Решение:**
```powershell
# Намерете процеса
netstat -ano | findstr :5001

# Спрете процеса (замете PID с номера от горната команда)
taskkill /PID [PID] /F
```

### Проблем: "Migration failed"
**Решение:**
```powershell
# Изтрийте базата и пуснете отново
Remove-Item LibrarySystem.db
dotnet ef database update
```

---

## 📂 Структура след инсталация

```
Библиотека/
├── LibrarySystem.db          ← База данни (SQLite)
├── Install.bat               ← Инсталационен скрипт
├── Install-LibrarySystem.ps1 ← PowerShell скрипт
├── README.md
├── LibraryManagementSystem/
│   └── bin/Release/          ← Компилиран код
└── [други папки...]
```

---

## 🎯 Следващи стъпки

След успешна инсталация:

1. **Разгледайте системата** - влезте като администратор
2. **Добавете книги** - Admin → Books → Create New
3. **Създайте събития** - Admin → Events → Create New
4. **Тествайте заемания** - влезте като член и заемете книга

---

## 📞 Помощ

При проблеми:
- Вижте `README.md` за детайлна документация
- Проверете `DOCUMENTATION.md` за техническа информация
- Свържете се: **136ikz@unibit.bg**

---

## ✅ Проверка на инсталацията

След стартиране трябва да видите:

```
info: Microsoft.Hosting.Lifetime[14]
      Now listening on: https://localhost:5001
info: Microsoft.Hosting.Lifetime[14]
      Now listening on: http://localhost:5000
info: Microsoft.Hosting.Lifetime[0]
      Application started. Press Ctrl+C to shut down.
```

Отворете браузър на `https://localhost:5001` и трябва да видите началната страница! 🎉

---

**Готово! Успешна работа!** 🚀

*Божидар Георгиев - 136ikz@unibit.bg*
